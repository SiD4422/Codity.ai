[build]
  base = "job-scheduler/frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so client-side routes (e.g. /queues/:id) don't 404 on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
